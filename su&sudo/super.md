# Switch user
```bash
su - userName // Land in users home dir
su username // land in current directory
```

# SUDO
Is short for super user do
1. For temporary access we can append `sudo` in front of any command
2. For Permanent changes we need to modify `/etc/sudoers` file <br>
*Command*: ```visudo```